export default class Stats extends React.Component {
  constructor(props){
    super(props);
  }

  render(){
    const table = "table text";
    const graph = "graph here";
    return (
      <div>
        {table}
        {graph}
      </div>
    );
  }
}